node_modules/
dist/
.astro/
.vercel/
.env
.env.local
.DS_Store
# Local-only fallback storage for accounts/submissions when no Vercel KV
# store is connected yet — see src/lib/kv.ts and README-ACCOUNTS-SETUP.md.
.data/
*.xlsx
*.docx

# Local Netlify folder
.netlify

# Backdrop art directions to compare by eye before promoting one to
# public/images/backdrop.webp — see scripts/generate-backdrop.js.
.backdrop-candidates/

# Dev-server output (e.g. `astro dev > server.stdout.log 2> server.stderr.log`).
# Local runtime noise, not source — and it can capture request paths.
*.log
